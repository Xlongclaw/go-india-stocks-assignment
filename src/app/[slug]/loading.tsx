/**
 * @file Loading.tsx
 * @description A component representing a loading screen or animation.
 */

import LoadingScreen from '@/components/loading'; // Importing the LoadingScreen component
import React from 'react';

/**
 * Loading component represents a loading screen or animation.
 * @returns - JSX element representing the Loading component.
 */
const Loading = () => {
  return (
    <LoadingScreen /> // Rendering the LoadingScreen component
  );
}

export default Loading;
